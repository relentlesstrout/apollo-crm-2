@props(['title', 'description'])

<div class="mt-10">
    <h1 class="text-3xl font-semibold text-slate-800">
        {{ $title }}
    </h1>
    <h2 class="text-lg font-medium text-slate-800 mt-2">
        {{ $description }}
    </h2>
</div>
